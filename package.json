{
  "name": "auth0-spa-auth-prereq",
  "version": "0.0.1",
  "description": "A javascript library for Single Page Applications that kicks off an authentication flow when authentication is a prerequisite for the SPA.",
  "main": "index.js",
  "scripts": {
    "build": "yarn run build:dev && yarn run build:dist",
    "build:dev": "webpack",
    "build:dist": "WEBPACK_ENV=dist webpack --optimize-minimize"
  },
  "repository": "git@github.com:auth0/auth0-spa-auth-prereq.git",
  "author": "Auth0",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.25.0",
    "babel-loader": "^7.1.0",
    "babel-preset-env": "^1.5.2",
    "webpack": "^3.0.0"
  },
  "dependencies": {
    "auth0-js": "^8.8.0",
    "moment": "^2.18.1",
    "uuid": "^3.1.0"
  }
}
